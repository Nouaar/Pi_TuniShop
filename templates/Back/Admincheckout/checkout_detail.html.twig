<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Checkout Details</title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css">
    <!-- Custom Styles -->
    <style>
        body {
            background-color: #f8f9fa;
        }
        .card {
            border-radius: 12px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        }
        .table-hover tbody tr:hover {
            background-color: #f1f1f1;
        }
        .total-price {
            font-size: 1.5rem;
            font-weight: bold;
            color: #28a745;
        }
    </style>
</head>
<body>

<div class="container my-5">
    <h1 class="text-center text-primary fw-bold">Checkout Details</h1>

    <div class="card shadow-lg border-0 mt-4">
        <div class="card-body p-4">
            <div class="mb-3">
                <p class="fw-semibold text-dark"><i class="bi bi-person-circle me-2 text-primary"></i> <strong>Customer Name:</strong> {{ checkout.firstName }} {{ checkout.secondName }}</p>
                <p class="fw-semibold text-dark"><i class="bi bi-envelope-fill me-2 text-danger"></i> <strong>Email:</strong> {{ checkout.email }}</p>
                <p class="fw-semibold text-dark"><i class="bi bi-geo-alt-fill me-2 text-success"></i> <strong>City:</strong> {{ checkout.city }}</p>
            </div>

            <h3 class="mt-4 text-secondary fw-bold">Products</h3>
            <div class="table-responsive">
                <table class="table table-bordered table-hover">
                    <thead class="table-primary">
                        <tr>
                            <th class="text-center">Product</th>
                            <th class="text-center">Price (€)</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% if checkout.idProduit is not null %}
                            <tr>
                                <td class="text-center text-dark fw-semibold">{{ checkout.idProduit.title }}</td>
                                <td class="text-center text-success fw-bold">{{ checkout.idProduit.price }} €</td>
                            </tr>
                        {% else %}
                            <tr>
                                <td colspan="2" class="text-center text-danger fw-bold">No products found</td>
                            </tr>
                        {% endif %}
                    </tbody>
                </table>
            </div>

            <h3 class="mt-4 text-dark fw-bold">
                <strong>Total Price:</strong> <span class="total-price">{{ total_price }} €</span>
            </h3>

            <div class="text-center mt-4">
                <a href="{{ path('back') }}#editcheckoutContent" class="btn btn-outline-secondary btn-lg px-4">
                    <i class="bi bi-arrow-left"></i> Back to List
                </a>
            </div>
        </div>
    </div>
</div>

<!-- Bootstrap JS (Optional for interactive components) -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>
