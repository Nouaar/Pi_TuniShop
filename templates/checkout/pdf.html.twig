<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Checkout Invoice</title>
    <style>
        body { font-family: Arial, sans-serif; }
        h1 { text-align: center; }
        table { width: 100%; border-collapse: collapse; margin-top: 20px; }
        th, td { border: 1px solid black; padding: 8px; text-align: left; }
        th { background-color: #f2f2f2; }
        .total { font-weight: bold; }
        .logo-container { text-align: center; margin-bottom: 20px; }
    </style>
</head>
<body>

    <!-- Logo at the top -->
    <div class="logo-container">
        <img src="{{ absolute_url(asset('img/logo.png')) }}" alt="Company Logo" style="max-width: 150px;">
    </div>

    <h1>Checkout Invoice</h1>
    <p><strong>Name:</strong> {{ checkout.firstName }} {{ checkout.secondName }}</p>
    <p><strong>Email:</strong> {{ checkout.email }}</p>
    <p><strong>City:</strong> {{ checkout.city }}</p>
    <p><strong>Checkout ID:</strong> {{ checkout.checkoutId }}</p>

    <h3>Products Ordered:</h3>
    <table>
        <thead>
            <tr>
                <th>Product</th>
                <th>Price</th>
            </tr>
        </thead>
        <tbody>
            {% for product in products %}
                <tr>
                    <td>{{ product.title }}</td>
                    <td>${{ product.price }}</td>
                </tr>
            {% endfor %}
        </tbody>
        <tfoot>
            <tr>
                <td class="total">Total Price:</td>
                <td class="total">${{ totalPrice }}</td>
            </tr>
        </tfoot>
    </table>
         <h3>QR Code:</h3>
    <img src="{{ qrCodeImage }}" alt="QR Code" style="width: 200px; height: 200px;">
</body>
</html>
